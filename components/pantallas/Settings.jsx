import { styles } from '../../styles/contenedores';
import {View, Text} from 'react-native'

export const Settings = () => {
    return <View style={styles.container}>
        <Text>aqui van los settings de usuario</Text>
        </View>
}